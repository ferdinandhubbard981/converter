change setData function, to not push until it encounters a byte that is != 0
//make it update colour only when necessary
check if targetx/y is more efficient than rundy/dx
strip rows and columns
perfect bands sketch is 91 bytes